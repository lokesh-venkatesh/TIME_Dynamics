1. Rewrite the efast algorithm with proper libraries and run it again to compare results
2. Find the formulae for the three (five) control variables - brainstorm with claude for what the exact parameter coefficients should be (make some notes and upload as a reference maybe)
3. Reproduce the exact treatment protocols and values and get the correct plot for figure 4
4. Finally, run this again and get the two values for different runs, and reconstruct supplementary figure S1
5. Lastly, see if you can obtain the dataset for the MCMC-DRAM thing and then run it for those 9 parameters.